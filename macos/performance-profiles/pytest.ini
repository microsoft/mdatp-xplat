[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -m "not integration"
markers =
    integration: live demo that drives mdatp and runs real builds (opt in with -m integration)
    slow: takes minutes (real clone + build)

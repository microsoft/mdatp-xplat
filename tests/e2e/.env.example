# tests/e2e/.env.example
# E2E Test Configuration Template
#
# Copy this file to .env and configure the paths to your JSON secret files.
# The JSON files should exist in the tests/e2e/ directory and will be
# synced to the VMs via rsync.
#
# SECURITY: The JSON files contain sensitive credentials and should be
# git-ignored. Only .env.example should be committed.

# Path to MDE onboarding JSON file (relative to tests/e2e/)
# This file is provided by Microsoft Defender for Endpoint
ONBOARDING_FILE=mdatp_onboard.json

# Path to MDE managed configuration JSON file (relative to tests/e2e/)
# This file contains device management policies
MANAGED_CONFIG_FILE=mdatp_managed.json

# Path to MDE offboarding JSON file (relative to tests/e2e/)
# Required for uninstall testing - HIGHLY SENSITIVE
OFFBOARDING_FILE=mdatp_offboard.json

# Optional: Custom test tags
TEST_TAGS=""
